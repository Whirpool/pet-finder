'use strict';

describe('pf-list directive', function () {

    var element,
        scope,
        compiled,
        caption,
        html;
    beforeEach(module('petFinder'));
    beforeEach(module('ng-search/app/partials/directives/pf-list.html'));


    it('should change class on mouseenter or mouseleave', function () {

    })
});